<?xml version="1.0" encoding="utf-8"?>
<ContentDialog x:Class="AirBridge.App.Pages.PairingDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               Title="Pair Device"
               CloseButtonText="Cancel"
               DefaultButton="None">

    <StackPanel Spacing="16" MinWidth="280">

        <!-- Status line -->
        <TextBlock x:Name="StatusText"
                   Text="Connecting\u2026"
                   HorizontalAlignment="Center"
                   Style="{StaticResource BodyTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

        <!-- Large animated PIN -->
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="24,16">
            <TextBlock x:Name="PinText"
                       Text="- - - - - -"
                       FontFamily="Consolas,Courier New,monospace"
                       FontSize="36"
                       FontWeight="Bold"
                       CharacterSpacing="500"
                       HorizontalAlignment="Center"
                       Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
        </Border>

        <TextBlock Text="Confirm this PIN on your Android device"
                   TextWrapping="Wrap"
                   HorizontalAlignment="Center"
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

        <ProgressBar x:Name="PairingProgress" IsIndeterminate="True"/>
    </StackPanel>
</ContentDialog>

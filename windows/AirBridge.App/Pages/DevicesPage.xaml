<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="AirBridge.App.Pages.DevicesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:models="using:AirBridge.Core.Models"
      Background="Transparent">

    <Grid Margin="24,16" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Devices"
                       Style="{StaticResource TitleTextBlockStyle}"
                       VerticalAlignment="Center"/>
            <Button Grid.Column="1"
                    Style="{StaticResource AccentButtonStyle}"
                    Command="{x:Bind ViewModel.ToggleScanCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <ProgressRing Width="16" Height="16"
                                  IsActive="{x:Bind ViewModel.IsScanning, Mode=OneWay}"
                                  Visibility="{x:Bind ViewModel.IsScanning, Mode=OneWay, Converter={StaticResource BoolToVisibility}}"/>
                    <FontIcon FontSize="16"
                              Glyph="{x:Bind ViewModel.IsScanning, Mode=OneWay, Converter={StaticResource BoolToScanIcon}}"/>
                    <TextBlock Text="{x:Bind ViewModel.IsScanning, Mode=OneWay, Converter={StaticResource BoolToScanText}}"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Info bar for status -->
        <InfoBar Grid.Row="1"
                 x:Name="StatusBar"
                 IsOpen="True"
                 IsClosable="False"
                 Severity="Informational"
                 Message="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"/>

        <!-- Device list -->
        <ListView Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.Devices}"
                  SelectionMode="None"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,4"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:DeviceInfo">
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Device type icon -->
                            <FontIcon Grid.Column="0"
                                      FontSize="28"
                                      Margin="0,0,16,0"
                                      Glyph="{x:Bind DeviceType, Converter={StaticResource DeviceTypeToIcon}}"
                                      Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                      VerticalAlignment="Center"/>

                            <!-- Device info -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="{x:Bind DeviceName}"
                                           Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           Style="{StaticResource CaptionTextBlockStyle}">
                                    <Run Text="{x:Bind IpAddress}"/>
                                    <Run Text=":"/>
                                    <Run Text="{x:Bind Port}"/>
                                </TextBlock>
                            </StackPanel>

                            <!-- Connect / Paired button -->
                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <Button Content="Paired"
                                        IsEnabled="False"
                                        Visibility="{x:Bind IsPaired, Converter={StaticResource BoolToVisibility}}"/>
                                <Button Content="Connect"
                                        Style="{StaticResource AccentButtonStyle}"
                                        Visibility="{x:Bind IsPaired, Converter={StaticResource BoolToVisibilityNegated}}"
                                        Tag="{x:Bind}"
                                        Click="ConnectButton_Click"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Bottom status bar -->
        <TextBlock Grid.Row="3"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   Style="{StaticResource CaptionTextBlockStyle}">
            <Run Text="{x:Bind ViewModel.Devices.Count, Mode=OneWay}"/>
            <Run Text=" device(s) found"/>
        </TextBlock>
    </Grid>
</Page>

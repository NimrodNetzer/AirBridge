<?xml version="1.0" encoding="utf-8"?>
<!--
  Configuration for AirBridgeAccessibilityService.
  This service is used solely to relay touch and key input events from the Windows
  mirror host to the Android device. It does NOT read screen content.

  canPerformGestures="true"       — required for GestureDescription dispatch (touch injection)
  canRetrieveWindowContent="false"— minimises permission scope; screen content is never read
  accessibilityEventTypes         — typeAllMask is required to keep the service alive;
                                    the service ignores all accessibility events it receives
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRequestTouchExplorationMode|flagRetrieveInteractiveWindows"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100" />
